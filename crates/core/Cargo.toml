[package]
name = "bti-core"
version = "0.1.0"
edition = "2021"

[dependencies]
redb = "2"
zstd = "0.13"
quinn = { version = "0.11", default-features = false, features = ["runtime-tokio"] }
thiserror = "2"
tracing = "0.1"
